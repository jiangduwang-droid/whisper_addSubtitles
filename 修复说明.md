# 视频字幕服务「卡在 5%」问题修复说明

> 2026-08-28 已部署到 124.220.30.182:8001 并端到端验证通过。
> 2026-08-29 新增「字幕人工校对」功能并部署验证（见文末）。

## 问题现象

上传视频成功后点击「开始处理」，进度条到 5% 后长时间不动，任务既不前进也不报错。

## 根因分析（共三层，均已实测确认）

### 根因 1：进度换算公式错误（显示层）

`flaskProject_ubuntu/app.py` 中转写阶段的进度映射写成了：

```python
progress_cb=lambda stage, pct, msg: task.update(stage, 5 + int(pct * 0.45), msg)
```

而 `transcribe.py` 上报的 `pct` 是 **0~1 之间的小数**（如 0.1、0.5、0.95）。`pct * 0.45` 的结果永远小于 1，`int()` 取整后**恒为 0**，所以转写全程进度永远等于 5%。

实测复现：`{5 + int(p * 0.45) for p in (0.1, 0.25, 0.5, 0.75, 0.95)}` 结果为 `{5}`。

### 根因 2：模型加载永久挂起（真正的卡死）

向线上服务（124.220.30.182:8001）实测上传 55 秒测试视频后轮询 10 分钟，任务始终停在：

```
stage=transcribing  progress=5  message=正在识别语音(可能较慢)
```

连「模型加载完成后的第一次上报」都没出现 —— 任务真正卡死在 `transcribe.py` 的 `_get_model()` 里，原因：

1. `WhisperModel(...)` 首次运行需从 HF 镜像下载模型，`huggingface_hub` 的下载**默认没有总超时**，网络停滞时无限等待；
2. 原代码把「下载+加载」放在**全局锁 `_MODEL_LOCK` 内**执行，一旦某次加载挂起，锁被永久持有，后续所有任务（无论选什么模型）都在锁上排队，**全部永远卡在 5%**；
3. 挂起不是异常，`except` 捕获不到，任务不会变成 failed，前端只能干等。

### 根因 3：联网版本校验挂起（部署时实测发现）

该服务器**外网完全不通**（hf-mirror.com 和 huggingface.co 均无法访问）。base 模型虽然已完整缓存在服务器上，但 `huggingface_hub` 加载时仍会尝试联网校验最新版本 → 无限挂起。

实测证据：直接 `WhisperModel('base')` 挂死 60 秒+；加 `local_files_only=True` 后 **3 秒加载成功**。

## 修复内容

### flaskProject_ubuntu/app.py
- 进度换算：转写 `5 + int(pct * 45)`（0~1 映射到 5~50），烧录 `50 + int(pct * 0.5)`（0~100 映射到 50~100），全程单调递增；
- 转写阶段文案改为「正在加载语音识别模型(首次运行需下载，可能较慢)」，给用户正确预期；
- 转写完成、烧录开始时补一条「语音识别完成，正在烧录字幕」(50%)；
- 新增启动时后台**预热默认模型**（`python app.py` 与 waitress 启动均覆盖），首个用户任务不再独自承担模型下载时间。

### flaskProject_ubuntu/transcribe.py
- 设置 `HF_HUB_DOWNLOAD_TIMEOUT=60`，下载停滞时以异常暴露而不是挂死；
- 模型加载移出全局锁：缓存锁只保护字典读写，按模型加 keyed 锁，**一个模型挂起不再拖死所有任务**；
- 加载放到子线程并带总超时（`WHISPER_MODEL_LOAD_TIMEOUT`，默认 600 秒）：超时抛出带排查建议的 `RuntimeError`，任务标记 failed，前端能看到明确错误；
- 并发同模型任务自动去重，只加载一次。

### flaskProject_ubuntu/templates/upload.html
- 轮询时检测「进度超过 2 分钟无变化」，在状态栏追加提示（首次运行需下载模型），继续轮询不中断；
- 上传上限提示改为根据后端配置动态显示（原来是写死的 2GB）。

### flaskProject_win/app.py、flaskProject_win/transcribe.py
- 同样的进度换算修复 + 模型加载超时保护（openai-whisper 的 `load_model` 同样可能因下载挂起）。

## 验证结果（14/14 通过）

| 验证项 | 结果 |
|---|---|
| 旧代码转写进度恒为 5（bug 复现） | 通过 |
| 新代码转写进度 9→16→27→38→47 递增 | 通过 |
| 烧录进度 50→62→75→87→100 | 通过 |
| 模拟完整流水线：5→47→50→100 单调推进，最终 done/100% | 通过 |
| 模型加载挂起：约 1 秒抛出带建议的超时错误（不再永久卡死） | 通过 |
| 正常加载、缓存复用、并发去重（3 线程只加载 1 次） | 通过 |

## 部署步骤（服务器 124.220.30.182）

```bash
# 1. 备份现有代码
cd /opt/addsub/code
cp app.py app.py.bak && cp transcribe.py transcribe.py.bak
cp templates/upload.html templates/upload.html.bak

# 2. 用修复版覆盖 flaskProject_ubuntu 下的
#    app.py / transcribe.py / templates/upload.html

# 3.（推荐）先手动预下载模型，避免首个任务等待下载
/home/ubuntu/addsub-venv/bin/python -c "from faster_whisper import WhisperModel; WhisperModel('tiny')"
/home/ubuntu/addsub-venv/bin/python -c "from faster_whisper import WhisperModel; WhisperModel('base')"   # 前端默认模型

# 4. 重启服务（必须重启！旧进程里有永久挂起的线程持有锁）
pkill -f 'python.*app.py'    # 或 kill 原 PID
# 然后按你原来的启动命令重启，例如：
WHISPER_MODEL=tiny PORT=8001 nohup /home/ubuntu/addsub-venv/bin/python app.py > server.log 2>&1 &

# 5. 验证：页面上传一个小视频，进度应在转写阶段从 5% 平滑上涨
```

注意：当前线上旧进程里已堆积多个永久卡死的任务（含本次排查的上传），重启后内存中的任务记录会清空，属正常现象。

## 新增可调环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `WHISPER_MODEL_LOAD_TIMEOUT` | 600 | 模型加载（含首次下载）总超时秒数 |
| `HF_HUB_DOWNLOAD_TIMEOUT` | 60 | 模型文件下载超时秒数（自动设置） |
| `WHISPER_WARMUP_MODEL` | 跟随 WHISPER_MODEL | 启动时预热的模型名 |

---

# 新功能：字幕人工校对（2026-08-29）

## 功能说明

转写完成后，流水线**暂停**在「校对字幕」阶段，页面弹出编辑器；用户对照视频画面调整字幕的时间轴和文本，确认后继续烧录。流程变为：

```
上传 → 识别语音 → 【人工校对】→ 烧录字幕 → 下载
```

## 编辑器能力

| 操作 | 说明 |
|---|---|
| 视频对照预览 | 左侧播放原始视频，字幕浮层按烧录样式（字号/颜色/描边）实时预览 |
| 定位 | 点击某条字幕「定位」，视频跳到该句开始处并播放 |
| 设起点 / 设终点 | 播放到句子的开始/结束画面时一键取当前播放时间，精准对齐 |
| 手改时间 | 直接编辑时间输入框（格式 `01:05.250` 或 `65.25`） |
| 改文本 | 每条字幕的文本框直接编辑 |
| 插入 / 删除 | 在当前播放位置插入新字幕；删除任意一条 |
| 播放跟随 | 播放中当前句自动高亮并滚动到可见区域 |
| 保存 / 确认 / 跳过 | 「仅保存」可多次保存；「确认并烧录」保存后继续；「跳过校对」按当前字幕直接烧录 |

## 后端接口

| 接口 | 说明 |
|---|---|
| `GET /api/subtitles/<id>` | 返回字幕列表（秒为单位）+ 样式配置 |
| `PUT /api/subtitles/<id>` | 保存校对（严格校验：时间合法、文本非空、≤3000 条、单条 ≤500 字；自动排序重编号；保存给任务目录续命） |
| `POST /api/confirm/<id>` | 置位确认信号，流水线从等待点恢复烧录（幂等） |
| `GET /api/video/<id>` | 原始视频流式预览（支持 Range 断点续传） |

## 行为细节

- 校对等待超时默认 2 小时（`EDIT_WAIT_TIMEOUT` 可调），超时按当前字幕自动继续，避免已完成转写被浪费；
- 确认后即使阶段标记未翻转也拒绝再写入字幕，避免和烧录抢文件；
- 并发确认幂等，烧录恰好执行一次；
- 人工校对只实现于 `flaskProject_ubuntu`（线上部署版本），Windows 版未同步此功能。

## 验证（26 项单元测试 + 线上端到端 + 真实浏览器实测）

- 单元测试 26/26：暂停点、字幕读取、非法输入拒绝、保存后烧录用新字幕、确认恢复、跳过、超时自动继续、并发幂等；
- 线上端到端：上传→转写→暂停在 48%→读字幕→改文本调时间→保存→确认→烧录完成→下载，服务器 srt 确认保存的是修改后内容；
- 浏览器实测：伪造文件选择走真实页面流程，编辑器渲染、定位/编辑/预览联动、确认烧录、下载按钮全部正常，无控制台报错。

---

# 2026-08-29 预览播放修复（校对页视频卡顿/只有声音）

## 问题现象

字幕校对阶段，预览视频框太小，且播放异常：非常卡顿、只有声音没有画面。

## 根因

1. **视频框太小**：编辑器布局给视频预留宽度不足，画面被压扁；
2. **只有声音没有画面**：上传的源视频编码是浏览器解不了的格式（HEVC/H.265、10bit、MKV/AVI 容器等）。浏览器能解 AAC 音频，解不了 HEVC 画面 → 表现为「只有声音、偶尔卡顿」；又因 moov 索引在文件尾部，边下边播时还要等全片下载完才能渲染，加重卡顿。

## 修复内容

### 后端 `app.py`

- 新增**预览副本生成**（转写完成进入校对阶段时后台线程执行，不阻塞流水线）：
  - 源片已是浏览器友好（H.264 8bit + AAC + MP4）且 moov 在头部 → 直接用原片，零等待；
  - 编码可播但缺 faststart → `-c copy` 流拷贝重封装，秒级完成；
  - 编码不兼容（HEVC/10bit/MKV 等）→ 转码 H.264 yuv420p + AAC，最高 720p，`+faststart`；
  - 转码超时上限 30 分钟（`PREVIEW_TRANSCODE_TIMEOUT` 可调），失败不阻断校对，前端回退原片。
- 新增接口：
  - `GET /api/preview/<id>` → 预览状态（none/generating/ready/failed）+ 就绪后的 URL；
  - `GET /api/preview/<id>/file` → 预览视频流式播放（支持 Range 断点续传）。

### 前端 `templates/upload.html` + `static/css/upload.css`

- **视频框放大**：校对页容器加宽，视频框自适应放大并保持 16:9；
- **全屏校对**：新增「⛶ 全屏校对」按钮，视频 + 字幕浮层一起进入全屏，便于逐帧对齐画面；
- **转码等待层**：预览转码期间显示「正在准备预览视频...」转圈提示，而不是让用户面对黑屏卡顿；
- **Range 拖动**：预览视频支持拖动进度条（依赖服务端 206 分段响应）。

## 验证（31 项单元测试 + 线上端到端）

- 单元测试 31/31（在原 26 项基础上新增预览相关 5 项）：四个转码分支、状态接口、预览文件 206 Range、未就绪 404；
- 线上端到端（124.220.30.182:8001）：构造 HEVC 测试视频（浏览器只能出声的画面缺失场景）上传 → 转写 16 秒完成 → 校对页预览状态 ready → 下载预览实测编码为 `h264 / yuv420p / aac`，moov 索引位于文件头部（faststart 生效），浏览器可直接流畅播放并拖动进度条。

## 回滚

服务器上保留了上一版备份：`app.py.bak_preview_0829`、`templates/upload.html.bak_preview_0829`、`static/css/upload.css.bak_preview_0829`（位于 `/opt/addsub/code`）。

---

# 新功能：字幕位置自定义（2026-08-29）

## 功能说明

校对页支持**手动调整字幕在画面中的位置**：

- **拖拽**：直接拖动视频里的字幕（或空档期的虚线占位框）到任意位置；
- **9 宫格预设**：左上/上中/右上/左中/正中/右中/左下/下中/右下一键定位；
- **恢复默认**：清除自定义位置，回到底部居中；
- **实时保存**：位置变化即通过 `PUT /api/style` 落盘，烧录时精确复现。

## 位置语义与烧录实现

- `pos_x / pos_y`（0~1）：字幕锚点在**视频内容区**中的相对坐标（信箱式留边已修正）；
- 带位置时烧录改走 **ASS 文件**：每条 `Dialogue` 用 `\pos(x,y)` 显式定位（PlayRes 384x288 基准），不再用只能表达边距的 `force_style`；
- **分区对齐**（锚点随位置区域变化，预览与烧录口径一一对应）：

| 区域 | pos_x | ASS Alignment | 锚点 | 文字延伸方向 |
|---|---|---|---|---|
| 左区 | ≤ 0.35 | 4 | 文本块左边中点 | 向右 |
| 中区 | 0.35 ~ 0.65 | 5 | 文本块中心 | 两侧对称 |
| 右区 | ≥ 0.65 | 6 | 文本块右边中点 | 向左 |

（三者垂直方向均锚文本块中心，与前端 `translate(?, -50%)` 一致）

- 无位置时走原有 `force_style` 路径（回归保护，行为与历史版本完全一致）。

## 长文本预换行（libass 不换行的坑）

**实测发现**：libass 对无空格的中文长句**不会自动换行**——`\pos` 定位下单行向两侧溢出画面且锚点漂移到画面中心；默认路径同样单行满宽。

**解决方案**：带位置烧录前在 Python 侧按字符宽度估算预换行（`\N`）：

- 字符宽度标定：字号 32 时实测中文每字 21.6 PlayRes 单位（系数 0.675），取 0.70 全宽系数 + 高估西文，宁可早换行也不溢出；
- 换行可用宽度随区域：左区 = 位置点到右缘、右区 = 左缘到位置点、中区 = 位置点两侧对称（各留 12 单位边距）；
- 英文按**整词**断行（不拆单词），CJK 逐字断行，用户手动换行保留；
- 前端 overlay 用同公式设置 `max-width` + CSS 自动换行，预览与烧录视觉一致。

## 后端接口

| 接口 | 说明 |
|---|---|
| `PUT /api/style/<id>` | 保存位置：`{"pos_x": 0~1, "pos_y": 0~1}`；`{"reset_position": true}` 恢复默认。校验范围 0.03~0.97，确认后 409 拒绝 |

## 验证（49 项单元测试 + 本地三区域烧录 + 线上端到端）

- 单元测试 49/49：位置接口（保存/回读/越界/非法/404/恢复/409）、ASS 生成（\pos 换算/分区 Alignment/字号/时间戳）、预换行（三区中文/英文整词/混合/手动换行/空文本）、烧录分支（带位置走 ass、无位置走 force_style 回归保护）、端到端流水线；
- 本地真实烧录三区域像素核对：左下（左缘 97px vs 期望 96px）、右下（右缘 541px vs 544px）、居中（中心 320px vs 320px），偏差均 < 0.5% 画面宽，长中文 3 行无溢出；
- 线上端到端（124.220.30.182:8001）：上传 → 校对阶段改长中文 + 设左下位置 → 烧录 → 下载抽帧：3 行换行 PASS、左缘 0.1516（目标 0.15）PASS、不溢出 PASS、垂直中心 0.8528（目标 0.85）PASS。

## 回滚

服务器备份：`/opt/addsub/bak_wrap_0829/`（含 addSubtitles.py / app.py / upload.html / upload.css 上一版）。

---

# 2026-08-29 修复：校对页预览视频不显示

## 问题现象

转写完成进入校对阶段（状态显示「字幕已生成，请校对时间轴与文本」），但预览视频不加载，编辑器面板也打不开。

## 根因

`templates/upload.html` 的 `openEditor()` 中，`const data` 声明在 `try` 块内（块级作用域），而后续恢复自定义位置的代码（`editPos = data.style...`）写在 `try` 块**外**——运行到此处直接抛 `ReferenceError: data is not defined`，函数中断，`startPreview()` 永远执行不到，`/api/preview/` 轮询从未发起。后端预览接口本身正常（视频已生成、`state: ready`）。

该 bug 是引入「位置恢复」功能时顺手带入的；JS 语法检查（`node --check`）无法发现作用域问题，后端单元测试也覆盖不到前端。

## 定位与验证方式

1. 服务器日志：任务调完 `GET /api/subtitles/` 后再无任何请求（连轮询 `/api/preview/` 都没有）→ 前端中断；
2. 浏览器实测：`await openEditor(taskId)` 捕获到 `ReferenceError: data is not defined`（含完整堆栈，指向 521 行）；
3. 修复后浏览器端到端复测：`openEditor` 无报错，视频 `src=/api/preview/...`、`readyState=4`、编辑器与字幕列表正常渲染、控制台无错误。

## 修复内容

`data` 提升为函数级 `let` 变量（`try` 内赋值，块外可用），一行改动。

## 注意

服务重启后内存中的任务记录会清空——重启前已上传但未确认烧录的任务会显示「任务不存在或已过期」，属预期行为，重新上传即可。

---

# 新功能：字幕样式调试（2026-08-29）

## 功能说明

校对页在「字幕位置」下方新增「字幕样式」控件行，可实时调试字幕外观：

- **字号**（8~100，与上传时同一 ASS 口径）；
- **文字颜色 / 描边颜色**（`#RRGGBB` 取色器）；
- **恢复默认**（32 / 白字 / 黑描边）；
- **即改即预览**：视频里的字幕实时变化；空档期（暂停/无字幕）常显「字幕样式预览」示例字样，随时能看到调整效果；
- **自动保存**：停手 600ms 后经 `PUT /api/style` 落盘，烧录时生效。

## 实现要点

- `PUT /api/style` 支持字段任意组合提交：`font_size`（非数字回退默认、超界夹紧、浮点截断）、`text_color`/`outline_color`（格式错误 400）、`reset_style`；位置与样式互不干扰（只改字号不清位置、只改位置不动样式）；空请求 400；确认后 409。
- 烧录两条路径都读 `task.style` 这三项：带位置走 ASS（`FontSize`/`PrimaryColour`/`OutlineColour` 写入 Style 行），无位置走 `force_style`（回归不变）。
- 预览字号换算：`font_size/288 × 视频显示高`（PlayRes 288 基准），颜色直接映射 CSS `color`/`-webkit-text-stroke`。
- 顺手修复：`_to_int` 此前对浮点输入（如 `45.9`）会静默回退默认值，现改为截断（45.9 → 45），上传与接口两条路径共用。

## 验证

- 单元测试 31/31：接口（字段组合/校验/夹紧/截断/reset/409/404/空请求）、烧录两路径（ASS 字号颜色 BGR 反序、force_style）、GET 回读；
- 本地真实烧录：52 号黄字黑描边 + 24 号红字蓝描边，核心色偏差 ≤2、字形高比 2.21 vs 字号比 2.17、位置精准；
- 线上端到端：上传 → 短文本 → 48 号黄字蓝描边左下位置 → 烧录 → 抽帧核对：颜色/描边/字号（36px vs 期望 37px）/锚点（左缘 0.184 vs 0.18、垂直中心 0.826 vs 0.82）全部 PASS；
- 浏览器实测：控件初始默认值、字号 56 → 预览 59px、颜色实时变化、防抖自动保存（后端回读 56/#FFD700/#00BFFF）、恢复默认按钮、控制台无错误。

## 已知行为（非 bug）

调大字号 + 长文本 + 低位置时，多行文本块可能超出画面底缘被裁剪（字号 48 在 PlayRes 288 下单行即占画面高 1/6）——属用户样式选择的结果；预换行仍保证水平方向不溢出。


# 中英混合识别与识别质量提升（2026-09-01）

## 问题现象

用户反馈：语音识别效果不佳，且无法同时识别中文与英文。

## 根因分析（TTS 生成带标准答案的测试音频实测）

用 edge-tts 生成纯中文/纯英文/中英交替/句内混说四类音频，以 CER（字符错误率）量化：

| 场景 | base+auto（线上默认） | 根因 |
|---|---|---|
| 中英交替（28s） | CER 69.6%，**3 句英文全部丢失** | Whisper 单次调用只支持一种语言：language 是全局解码约束，检测到主导语言后非主导语言的句子被直接跳过 |
| 纯中文 | CER 23.1%，输出繁体、无标点 | auto 路径没传 initial_prompt，中文无简体引导（显式 zh 时有） |
| 句内混说 | CER 33.7%（Docker→Dockeur、Kubernetes→Copernetic） | base 模型容量不足；medium 同场景 CER 6.0% |

补充实测：medium+en 对中英交替反而全对（CER 0%），medium+zh 却丢英文——说明"指定语言"对混合识别的行为不可靠，不能作为修复方向。

## 修复方案

### 1. 新增「中英混合」语言选项（transcribe.py）

上传页识别语言新增 `中英混合（zh+en）`。实现：VAD 切句 → 逐句独立检测语言（限定 zh/en）→ 按句语言转写 → 时间轴平移拼回全片。两个关键参数：

- `speech_pad_ms=120`（默认 400 会把两侧各 0.4s 的句间静音填满，相邻句粘连成整段后语言检测退回主导语言——修复前实测复现丢英文）；
- 碎片合并阈值 0.2s（双语视频的句间停顿就是语言切换点，不能当同句合并）。

### 2. auto 模式自动加简体引导（transcribe.py）

`_probe_language` 只取前 30s 探测语言，检测到中文时自动加 `以下是普通话的句子。` 引导（与显式 zh 同口径），修复繁体+无标点问题。探测失败不影响主流程。

### 3. 兜底链路

VAD 无结果或双语产出为空时，退回单语言 zh 转写，保证任何输入都有字幕产出。

## 验证（修复后）

- 本地：mixed CER 69.6% → **0.0%**（base 与 medium 均 7 句全对）；auto 纯中文 23.1% → 0.0%（无繁体残留）；
- 线上端到端：上传 mixed.mp4 选「中英混合」+ base 模型，逐段识别 7/7（中/英交替检测正确），最终 7 条字幕中英齐全，时间轴正确；
- 线上 auto 模式：纯中文输出全部简体（修复前为繁体）。

## 使用建议

- 中英混合视频：识别语言选「中英混合」；
- 识别质量优先：模型选 medium（句内混说场景 CER 从 33.7% 降到 6.0%，专有名词 Docker/Kubernetes 明显更准）；CPU int8 下约 3 倍实时速度，可接受。

## 性能优化：语言段合并（2026-09-01 追加）

双语路径改为三阶段：逐段语言检测 → **相邻同语言段合并**（单组 ≤30s whisper 窗口）→ 逐大段转写。真实双语视频的语言分布是「大块连续」的（整段中文讲解 + 整段英文对白），合并后转写调用次数从「句数级」降到「语言切换次数级」。

实测：56.9s 长音频（4 句中文 + 7 句中英交替 + 3 句英文），转写调用 15 → 8 次，3.6 倍实时速度，12 条字幕中英文全部正确、时间轴平移无误。带粉噪场景双语模式 CER 仍为 0%（auto 模式噪声下依旧丢英文，属预期——单语言约束未变，混合视频请选「中英混合」）。
