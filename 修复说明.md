# 视频字幕服务「卡在 5%」问题修复说明

> 2026-08-28 已部署到 124.220.30.182:8001 并端到端验证通过。
> 2026-08-29 新增「字幕人工校对」功能并部署验证（见文末）。

## 问题现象

上传视频成功后点击「开始处理」，进度条到 5% 后长时间不动，任务既不前进也不报错。

## 根因分析（共三层，均已实测确认）

### 根因 1：进度换算公式错误（显示层）

`flaskProject_ubuntu/app.py` 中转写阶段的进度映射写成了：

```python
progress_cb=lambda stage, pct, msg: task.update(stage, 5 + int(pct * 0.45), msg)
```

而 `transcribe.py` 上报的 `pct` 是 **0~1 之间的小数**（如 0.1、0.5、0.95）。`pct * 0.45` 的结果永远小于 1，`int()` 取整后**恒为 0**，所以转写全程进度永远等于 5%。

实测复现：`{5 + int(p * 0.45) for p in (0.1, 0.25, 0.5, 0.75, 0.95)}` 结果为 `{5}`。

### 根因 2：模型加载永久挂起（真正的卡死）

向线上服务（124.220.30.182:8001）实测上传 55 秒测试视频后轮询 10 分钟，任务始终停在：

```
stage=transcribing  progress=5  message=正在识别语音(可能较慢)
```

连「模型加载完成后的第一次上报」都没出现 —— 任务真正卡死在 `transcribe.py` 的 `_get_model()` 里，原因：

1. `WhisperModel(...)` 首次运行需从 HF 镜像下载模型，`huggingface_hub` 的下载**默认没有总超时**，网络停滞时无限等待；
2. 原代码把「下载+加载」放在**全局锁 `_MODEL_LOCK` 内**执行，一旦某次加载挂起，锁被永久持有，后续所有任务（无论选什么模型）都在锁上排队，**全部永远卡在 5%**；
3. 挂起不是异常，`except` 捕获不到，任务不会变成 failed，前端只能干等。

### 根因 3：联网版本校验挂起（部署时实测发现）

该服务器**外网完全不通**（hf-mirror.com 和 huggingface.co 均无法访问）。base 模型虽然已完整缓存在服务器上，但 `huggingface_hub` 加载时仍会尝试联网校验最新版本 → 无限挂起。

实测证据：直接 `WhisperModel('base')` 挂死 60 秒+；加 `local_files_only=True` 后 **3 秒加载成功**。

## 修复内容

### flaskProject_ubuntu/app.py
- 进度换算：转写 `5 + int(pct * 45)`（0~1 映射到 5~50），烧录 `50 + int(pct * 0.5)`（0~100 映射到 50~100），全程单调递增；
- 转写阶段文案改为「正在加载语音识别模型(首次运行需下载，可能较慢)」，给用户正确预期；
- 转写完成、烧录开始时补一条「语音识别完成，正在烧录字幕」(50%)；
- 新增启动时后台**预热默认模型**（`python app.py` 与 waitress 启动均覆盖），首个用户任务不再独自承担模型下载时间。

### flaskProject_ubuntu/transcribe.py
- 设置 `HF_HUB_DOWNLOAD_TIMEOUT=60`，下载停滞时以异常暴露而不是挂死；
- 模型加载移出全局锁：缓存锁只保护字典读写，按模型加 keyed 锁，**一个模型挂起不再拖死所有任务**；
- 加载放到子线程并带总超时（`WHISPER_MODEL_LOAD_TIMEOUT`，默认 600 秒）：超时抛出带排查建议的 `RuntimeError`，任务标记 failed，前端能看到明确错误；
- 并发同模型任务自动去重，只加载一次。

### flaskProject_ubuntu/templates/upload.html
- 轮询时检测「进度超过 2 分钟无变化」，在状态栏追加提示（首次运行需下载模型），继续轮询不中断；
- 上传上限提示改为根据后端配置动态显示（原来是写死的 2GB）。

### flaskProject_win/app.py、flaskProject_win/transcribe.py
- 同样的进度换算修复 + 模型加载超时保护（openai-whisper 的 `load_model` 同样可能因下载挂起）。

## 验证结果（14/14 通过）

| 验证项 | 结果 |
|---|---|
| 旧代码转写进度恒为 5（bug 复现） | 通过 |
| 新代码转写进度 9→16→27→38→47 递增 | 通过 |
| 烧录进度 50→62→75→87→100 | 通过 |
| 模拟完整流水线：5→47→50→100 单调推进，最终 done/100% | 通过 |
| 模型加载挂起：约 1 秒抛出带建议的超时错误（不再永久卡死） | 通过 |
| 正常加载、缓存复用、并发去重（3 线程只加载 1 次） | 通过 |

## 部署步骤（服务器 124.220.30.182）

```bash
# 1. 备份现有代码
cd /opt/addsub/code
cp app.py app.py.bak && cp transcribe.py transcribe.py.bak
cp templates/upload.html templates/upload.html.bak

# 2. 用修复版覆盖 flaskProject_ubuntu 下的
#    app.py / transcribe.py / templates/upload.html

# 3.（推荐）先手动预下载模型，避免首个任务等待下载
/home/ubuntu/addsub-venv/bin/python -c "from faster_whisper import WhisperModel; WhisperModel('tiny')"
/home/ubuntu/addsub-venv/bin/python -c "from faster_whisper import WhisperModel; WhisperModel('base')"   # 前端默认模型

# 4. 重启服务（必须重启！旧进程里有永久挂起的线程持有锁）
pkill -f 'python.*app.py'    # 或 kill 原 PID
# 然后按你原来的启动命令重启，例如：
WHISPER_MODEL=tiny PORT=8001 nohup /home/ubuntu/addsub-venv/bin/python app.py > server.log 2>&1 &

# 5. 验证：页面上传一个小视频，进度应在转写阶段从 5% 平滑上涨
```

注意：当前线上旧进程里已堆积多个永久卡死的任务（含本次排查的上传），重启后内存中的任务记录会清空，属正常现象。

## 新增可调环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `WHISPER_MODEL_LOAD_TIMEOUT` | 600 | 模型加载（含首次下载）总超时秒数 |
| `HF_HUB_DOWNLOAD_TIMEOUT` | 60 | 模型文件下载超时秒数（自动设置） |
| `WHISPER_WARMUP_MODEL` | 跟随 WHISPER_MODEL | 启动时预热的模型名 |

---

# 新功能：字幕人工校对（2026-08-29）

## 功能说明

转写完成后，流水线**暂停**在「校对字幕」阶段，页面弹出编辑器；用户对照视频画面调整字幕的时间轴和文本，确认后继续烧录。流程变为：

```
上传 → 识别语音 → 【人工校对】→ 烧录字幕 → 下载
```

## 编辑器能力

| 操作 | 说明 |
|---|---|
| 视频对照预览 | 左侧播放原始视频，字幕浮层按烧录样式（字号/颜色/描边）实时预览 |
| 定位 | 点击某条字幕「定位」，视频跳到该句开始处并播放 |
| 设起点 / 设终点 | 播放到句子的开始/结束画面时一键取当前播放时间，精准对齐 |
| 手改时间 | 直接编辑时间输入框（格式 `01:05.250` 或 `65.25`） |
| 改文本 | 每条字幕的文本框直接编辑 |
| 插入 / 删除 | 在当前播放位置插入新字幕；删除任意一条 |
| 播放跟随 | 播放中当前句自动高亮并滚动到可见区域 |
| 保存 / 确认 / 跳过 | 「仅保存」可多次保存；「确认并烧录」保存后继续；「跳过校对」按当前字幕直接烧录 |

## 后端接口

| 接口 | 说明 |
|---|---|
| `GET /api/subtitles/<id>` | 返回字幕列表（秒为单位）+ 样式配置 |
| `PUT /api/subtitles/<id>` | 保存校对（严格校验：时间合法、文本非空、≤3000 条、单条 ≤500 字；自动排序重编号；保存给任务目录续命） |
| `POST /api/confirm/<id>` | 置位确认信号，流水线从等待点恢复烧录（幂等） |
| `GET /api/video/<id>` | 原始视频流式预览（支持 Range 断点续传） |

## 行为细节

- 校对等待超时默认 2 小时（`EDIT_WAIT_TIMEOUT` 可调），超时按当前字幕自动继续，避免已完成转写被浪费；
- 确认后即使阶段标记未翻转也拒绝再写入字幕，避免和烧录抢文件；
- 并发确认幂等，烧录恰好执行一次；
- 人工校对只实现于 `flaskProject_ubuntu`（线上部署版本），Windows 版未同步此功能。

## 验证（26 项单元测试 + 线上端到端 + 真实浏览器实测）

- 单元测试 26/26：暂停点、字幕读取、非法输入拒绝、保存后烧录用新字幕、确认恢复、跳过、超时自动继续、并发幂等；
- 线上端到端：上传→转写→暂停在 48%→读字幕→改文本调时间→保存→确认→烧录完成→下载，服务器 srt 确认保存的是修改后内容；
- 浏览器实测：伪造文件选择走真实页面流程，编辑器渲染、定位/编辑/预览联动、确认烧录、下载按钮全部正常，无控制台报错。

---

# 2026-08-29 预览播放修复（校对页视频卡顿/只有声音）

## 问题现象

字幕校对阶段，预览视频框太小，且播放异常：非常卡顿、只有声音没有画面。

## 根因

1. **视频框太小**：编辑器布局给视频预留宽度不足，画面被压扁；
2. **只有声音没有画面**：上传的源视频编码是浏览器解不了的格式（HEVC/H.265、10bit、MKV/AVI 容器等）。浏览器能解 AAC 音频，解不了 HEVC 画面 → 表现为「只有声音、偶尔卡顿」；又因 moov 索引在文件尾部，边下边播时还要等全片下载完才能渲染，加重卡顿。

## 修复内容

### 后端 `app.py`

- 新增**预览副本生成**（转写完成进入校对阶段时后台线程执行，不阻塞流水线）：
  - 源片已是浏览器友好（H.264 8bit + AAC + MP4）且 moov 在头部 → 直接用原片，零等待；
  - 编码可播但缺 faststart → `-c copy` 流拷贝重封装，秒级完成；
  - 编码不兼容（HEVC/10bit/MKV 等）→ 转码 H.264 yuv420p + AAC，最高 720p，`+faststart`；
  - 转码超时上限 30 分钟（`PREVIEW_TRANSCODE_TIMEOUT` 可调），失败不阻断校对，前端回退原片。
- 新增接口：
  - `GET /api/preview/<id>` → 预览状态（none/generating/ready/failed）+ 就绪后的 URL；
  - `GET /api/preview/<id>/file` → 预览视频流式播放（支持 Range 断点续传）。

### 前端 `templates/upload.html` + `static/css/upload.css`

- **视频框放大**：校对页容器加宽，视频框自适应放大并保持 16:9；
- **全屏校对**：新增「⛶ 全屏校对」按钮，视频 + 字幕浮层一起进入全屏，便于逐帧对齐画面；
- **转码等待层**：预览转码期间显示「正在准备预览视频...」转圈提示，而不是让用户面对黑屏卡顿；
- **Range 拖动**：预览视频支持拖动进度条（依赖服务端 206 分段响应）。

## 验证（31 项单元测试 + 线上端到端）

- 单元测试 31/31（在原 26 项基础上新增预览相关 5 项）：四个转码分支、状态接口、预览文件 206 Range、未就绪 404；
- 线上端到端（124.220.30.182:8001）：构造 HEVC 测试视频（浏览器只能出声的画面缺失场景）上传 → 转写 16 秒完成 → 校对页预览状态 ready → 下载预览实测编码为 `h264 / yuv420p / aac`，moov 索引位于文件头部（faststart 生效），浏览器可直接流畅播放并拖动进度条。

## 回滚

服务器上保留了上一版备份：`app.py.bak_preview_0829`、`templates/upload.html.bak_preview_0829`、`static/css/upload.css.bak_preview_0829`（位于 `/opt/addsub/code`）。
